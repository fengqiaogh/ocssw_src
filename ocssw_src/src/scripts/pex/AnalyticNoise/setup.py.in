from distutils.core import setup

setup(name='AnalyticNoise',
      version='${PACKAGE_VERSION}',
      package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}' },
      scripts=['$ENV{OCSSWROOT}/src/scripts/AnalyticNoise2.py']
)
