from distutils.core import setup

setup(name='spacetrack_tle',
      version='${PACKAGE_VERSION}',
      package_dir={ '': '${CMAKE_CURRENT_BINARY_DIR}' },
      scripts=['$ENV{OCSSWROOT}/src/scripts/spacetrack_tles.py']
)
