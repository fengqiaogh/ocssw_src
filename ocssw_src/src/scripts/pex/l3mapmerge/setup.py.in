from distutils.core import setup

setup(name='l3mapmerge',
      version='${PACKAGE_VERSION}',
      package_dir={ '': '${CMAKE_CURRENT_BINARY_DIR}' },
      scripts=['$ENV{OCSSWROOT}/src/scripts/l3mapmerge.py']
)
