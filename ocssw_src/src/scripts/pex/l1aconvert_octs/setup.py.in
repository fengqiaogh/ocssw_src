from distutils.core import setup

setup(name='l1aconvert_octs',
      version='${PACKAGE_VERSION}',
      package_dir={ '': '${CMAKE_CURRENT_BINARY_DIR}' },
      scripts=['$ENV{OCSSWROOT}/src/scripts/l1aconvert_octs.py']
)
