from distutils.core import setup

setup(name='mapgen_overlay',
      version='${PACKAGE_VERSION}',
      package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}' },
      scripts=['$ENV{OCSSWROOT}/src/scripts/mapgen_overlay.py']
)
